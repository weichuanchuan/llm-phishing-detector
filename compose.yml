services:
  app:
    container_name: mailcheck-python
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000

networks:
  default:
    driver: bridge
